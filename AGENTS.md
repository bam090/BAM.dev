# BAM.dev 에이전트 운영 규칙

## 제품 원칙

- 초보 개발자가 개념을 자신의 말로 설명하고 직접 구현하도록 돕는다.
- 정답을 먼저 노출하기보다 개념, 관찰 지점, 단계별 힌트를 우선한다.
- 실행하지 않은 코드의 결과를 지어내지 않는다.
- 브라우저에 전달되는 테스트는 비밀 테스트라고 표현하지 않는다.
- 접근성과 모바일 사용성을 완료 조건에 포함한다.

## 기술 경계

- 애플리케이션은 HTML, CSS, Vanilla JavaScript로 구현한다.
- 1~3차는 외부 서버 없이 로컬에서 완전히 사용할 수 있어야 한다.
- 사용자 진도는 저장소 인터페이스 뒤에 두고 현재는 `localStorage` 구현을 사용한다.
- 콘텐츠와 사용자 상태를 분리한다. 콘텐츠 항목은 안정적인 ID로 연결한다.
- 외부 의존성은 이점과 비용을 문서화한 뒤 정확한 버전으로 추가한다.
- Supabase 키, 서비스 역할 키, 비밀값을 저장소에 커밋하지 않는다.

## 변경과 검증

- 원본 참고 자료는 읽기 전용이다. 복제본의 출처를 문서화한다.
- 변경 단위마다 `npm run check`를 통과시킨다.
- 콘텐츠 추가 시 스키마 검증과 ID/순서/파일 경로 검증을 함께 실행한다.
- 사용자가 만든 기존 변경은 삭제하거나 덮어쓰지 않는다.

## Git 흐름

- `main`은 빈 초기 커밋만 유지한다.
- 통합 브랜치는 `dev`다.
- 0~3차는 `codex/js-foundation`에서 진행하고 `dev` 대상 Draft PR 하나로 관리한다.
- 3차 통합 검증 전에는 PR을 병합하지 않는다.
- 이후 기능 브랜치는 `codex/<feature>` 형식을 사용한다.
- force push, `reset --hard`, `clean`을 사용하지 않는다.
- 커밋과 PR 설명은 한국어로 작성한다.

## 전문 에이전트 책임

- 콘텐츠: 교안의 정확성, 출처, 난이도, 개념 ID를 검증한다.
- 프론트엔드: 반응형 UI, 키보드 흐름, 상태 표현을 구현한다.
- 평가: 정답과 오답 근거, 테스트 독립성, 실패 설명을 검증한다.
- 데이터: 저장소 경계를 지키고 이후 Supabase 마이그레이션 가능성을 유지한다.
- QA: 콘텐츠, 단위 테스트, 브라우저 smoke test, 접근성을 함께 확인한다.

## 역할별 계약

- 전문 역할을 배정하기 전에 [역할 계약](agents/roles.md)의 해당 절과 [작업 카드](agents/task-card.md)를 함께 제공한다.
- 역할의 기본 범위보다 작업 카드의 허용 경로·금지 경로·외부 작업 권한이 우선한다.
- 기본 검증 흐름은 기능 구현 → 테스트 → 실패 시 원 구현 역할에 반환 → 재검증 → 콘텐츠 검증 또는 총괄 통합 → Git 통합이다.
